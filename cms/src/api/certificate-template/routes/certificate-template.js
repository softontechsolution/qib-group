'use strict';

/**
 * certificate-template router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::certificate-template.certificate-template');
