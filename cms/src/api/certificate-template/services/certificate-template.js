'use strict';

/**
 * certificate-template service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::certificate-template.certificate-template');
